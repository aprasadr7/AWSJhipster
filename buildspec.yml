version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - ./mvnw -Pprod clean verify
    post_build:
      commands:
        - echo Build Completed
artifacts:
  files:
    - target/*.jar
    - scripts/*.sh
    - appspec.yml
  discard-paths: yes